# CineTime Movie Booking System Application Properties
spring.application.name=cinetime-project

#############################################################
# Server Configuration
server.port=${SERVER_PORT}
server.error.include-binding-errors=always
server.error.include-message=always
server.error.include-stacktrace=never

###########################################################
# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

###########################################################
# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.bytecode.use_reflection_optimizer=false
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# Hibernate specific settings
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

###########################################################
# Spring MVC Configuration
spring.mvc.pathmatch.matching-strategy=ANT_PATH_MATCHER

###########################################################
# JWT Security Configuration
cinetime.app.jwtSecret=${CINETIME_APP_JWTSECRET}
cinetime.app.jwtExpirationMs=86400000
# 24 hours = 86400000 ms

###########################################################
# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true
cinetime.default-region=TR

###########################################################
# Google Client ID for Google Login (Will be replaced with real ID)
app.google.client-id=${APP_GOOGLE_CLIENT_ID}


###########################################################
# Email (SendGrid SMTP)
# ---- MAIL (SendGrid SMTP) ----
app.mail.from=cinetimeprojectteam22025@gmail.com
app.mail.reset.subject=CineTime - Password Reset Code
app.mail.reset.template=<p>Hello,</p><p>Your code is: <b>%s</b></p><p>Please use it within 10 minutes.</p>

# SendGrid SMTP (587 + STARTTLS)
spring.mail.properties.mail.smtp.starttls.required=true
#spring.mail.properties.mail.smtp.ssl.trust=*
spring.mail.properties.mail.smtp.ssl.trust=smtp.sendgrid.net
# isterseniz hepsine guven: spring.mail.properties.mail.smtp.ssl.trust=*


spring.mail.host=smtp.sendgrid.net
spring.mail.port=587
spring.mail.username=apikey
spring.mail.password=${SENDGRID_API_KEY}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Tan? i�in log
spring.mail.properties.mail.debug=true
logging.level.org.springframework.mail=TRACE

###########################################################
# CineTime Business Logic Configuration
cinetime.ticket.reservation.timeout.minutes=15
cinetime.movie.rating.min=0.0
cinetime.movie.rating.max=10.0
cinetime.payment.timeout.minutes=30

###########################################################
# Logging Configuration
logging.file.name=log/cinetime.log
logging.pattern.file="%d %-5level [%thread] %logger:%msg%n"
logging.level.org.springframework.web=WARN
logging.level.org.hibernate=INFO
logging.level.com.cinetime=DEBUG
logging.level.org.springframework.security=DEBUG

# Console logging pattern
logging.pattern.console="%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

###########################################################
# API Documentation (Swagger)
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.packages-to-scan=com.cinetime.controller

###########################################################
# Jackson Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=Europe/Istanbul

###########################################################
# Development Profile Specific
# spring.profiles.active=dev

###########################################################
# Actuator Configuration (for monitoring)
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized


